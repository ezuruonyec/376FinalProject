using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //TODO: AI 
        // if player in area approach them 
        //if player in range, attack them 
        //add trigger collider to sword to attack player
        //if hit by player sword, take damage
        //if health <= 0, die
        
    }
}
